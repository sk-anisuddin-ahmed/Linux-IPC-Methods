## Client-Server Unix Sockets

Start the server in one terminal:

```bash
./client_server server
```

In another terminal, run the client:

```bash
./client_server client
```

The client will send a message and receive the echo from the server.